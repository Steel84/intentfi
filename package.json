{
  "name": "intentfi",
  "version": "0.1.0",
  "description": "AI-assisted on-chain intent execution system — LLM proposes, policy decides, human approves, blockchain executes.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "viem": "^2.0.0",
    "wagmi": "^2.0.0",
    "@tanstack/react-query": "^5.0.0",
    "@connectkit/connectkit": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.2.0",
    "vitest": "^1.0.0",
    "eslint": "^8.56.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  }
}
